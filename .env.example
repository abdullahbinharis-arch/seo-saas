# SEO SaaS — Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── Required ──────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=your-anthropic-api-key-here
SERPAPI_KEY=your-serpapi-key-here
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your-jwt-secret-here

# ── Optional (defaults shown) ──────────────────────────────────────────────────
CLAUDE_MODEL=claude-sonnet-4-6
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
RATE_LIMIT_PER_MIN=10
PORT=8000

# ── Phase 1 Additions ─────────────────────────────────────────────────────────
# Google PageSpeed Insights — free API key raises quota from 25/day to 25,000/day
# Get one free: https://console.cloud.google.com → APIs → PageSpeed Insights API → Create credentials
# Works without a key but hits quota quickly in testing.
PAGESPEED_API_KEY=

# Moz Links API — optional, adds verified DA/PA/backlink data (10 queries/month free)
# Get free access: https://moz.com/products/mozpro/billing (free tier available)
# Without these keys, the backlink agent falls back to Claude estimation (still useful).
MOZ_ACCESS_ID=
MOZ_SECRET_KEY=

# Email whitelist — comma-separated emails that bypass rate limiting
RATE_LIMIT_WHITELIST=
